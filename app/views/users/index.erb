<h1>Users page</h1>
<h2>These are users on the app</h2>
<h3>Click on a user to learn more about them</h3>
<ul>
  <% @users.each do |user|  %>
<li>
  <%= link_to user.name, user_path(user)%>
</li>
    <br>
   <% end %>
</ul>
